{
  "name": "sst-cloud",
  "version": "0.1.1",
  "description": "sst-cloud.com interface",
  "main": "src/index.js",
  "files": [
    "src/*"
  ],
  "scripts": {
    "test": "ava",
    "lint": "eslint ./",
    "coverage": "nyc ava && nyc report --reporter=html",
    "travis": "nyc ava && nyc report --reporter=text-lcov | coveralls"
  },
  "author": "Igor Kalashnikov <igor.kalashnikov@me.com> (https://github.com/silentroach)",
  "license": "MIT",
  "engines": {
    "node": ">=9.0.0"
  },
  "dependencies": {
    "request": "^2.88.0"
  },
  "devDependencies": {
    "ava": "^0.25.0",
    "coveralls": "^3.0.2",
    "doctrine": "^2.1.0",
    "eslint": "^5.3.0",
    "eslint-plugin-ava": "^5.1.0",
    "espree": "^4.0.0",
    "faker": "^4.1.0",
    "nock": "^9.6.1",
    "nyc": "^12.0.2"
  }
}
